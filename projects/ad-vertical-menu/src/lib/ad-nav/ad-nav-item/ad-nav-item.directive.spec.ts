import { AdNavItemDirective } from './ad-nav-item.directive';

describe('AdNavItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AdNavItemDirective();
    expect(directive).toBeTruthy();
  });
});
