import { AdNavNesterDirective } from './ad-nav-nester.directive';

describe('AdNavNesterDirective', () => {
  it('should create an instance', () => {
    const directive = new AdNavNesterDirective();
    expect(directive).toBeTruthy();
  });
});
